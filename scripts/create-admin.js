require('dotenv').config();const {createUser}=require('../src/services/users');const prisma=require('../src/database/prisma');const [username,email,password]=process.argv.slice(2);if(!username||!email||!password){console.error('Usage: npm run admin:create -- <username> <email> <password>');process.exit(1)}createUser({username,email,password,role:'admin',plan:'PRO'}).then(u=>console.log(`Admin created: ${u.email}`)).catch(e=>{console.error(e.message);process.exitCode=1}).finally(()=>prisma.$disconnect());
