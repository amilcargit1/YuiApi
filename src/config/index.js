const env=process.env; const list=v=>(v||'').split(',').map(s=>s.trim()).filter(Boolean); module.exports={nodeEnv:env.NODE_ENV||'development',port:Number(env.PORT||3000),appUrl:env.APP_URL||`http://localhost:${env.PORT||3000}`,jwtSecret:env.JWT_SECRET||'development-only-change-me',jwtExpiresIn:env.JWT_EXPIRES_IN||'7d',corsOrigins:list(env.CORS_ORIGINS||'http://localhost:3000'),redisUrl:env.REDIS_URL||'',trustProxy:Number(env.TRUST_PROXY||0),logLevel:env.LOG_LEVEL||'info'};
