const rateLimit=require('express-rate-limit');const PLANS=require('../utils/plans');const cache=new Map();function planRateLimit(req,res,next){const p=PLANS[req.user?.plan||'FREE'];if(!cache.has(p.ratePerMinute))cache.set(p.ratePerMinute,rateLimit({windowMs:60000,limit:p.ratePerMinute,standardHeaders:'draft-8',legacyHeaders:false,keyGenerator:r=>req.user?.id||r.ip,handler:(r,s)=>s.status(429).json({status:false,code:429,message:'Rate limit exceeded',data:null})}));return cache.get(p.ratePerMinute)(req,res,next)}module.exports={planRateLimit};
