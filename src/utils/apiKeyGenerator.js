'use strict';
const crypto=require('crypto');const {customAlphabet}=require('nanoid');const ALPHABET='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';const nanoid=customAlphabet(ALPHABET,32);const KEY_PREFIX='YUI_';function generateApiKey(){const rawKey=`${KEY_PREFIX}${nanoid()}`;const keyHash=hashApiKey(rawKey);const keyPrefix=rawKey.slice(0,12);return{rawKey,keyHash,keyPrefix};}function hashApiKey(rawKey){return crypto.createHash('sha256').update(rawKey).digest('hex');}function isValidKeyFormat(rawKey){return typeof rawKey==='string'&&rawKey.startsWith(KEY_PREFIX)&&rawKey.length>=20;}module.exports={generateApiKey,hashApiKey,isValidKeyFormat,KEY_PREFIX};
