const router=require('express').Router();const {z}=require('zod');const {requireAuth}=require('../middleware/auth');const {success,error}=require('../utils/response');const {createKey,listKeys}=require('../services/apiKeys');const prisma=require('../database/prisma');router.use(requireAuth);router.get('/',async(req,res,next)=>{try{return success(res,await listKeys(req.user.sub),'API keys')}catch(e){next(e)}});router.post('/',async(req,res,next)=>{try{const p=z.object({name:z.string().min(1).max(80)}).safeParse(req.body);if(!p.success)return error(res,400,'Invalid key name');const k=await createKey(req.user.sub,p.data.name);return success(res,{id:k.id,name:k.name,key:k.key,createdAt:k.createdAt},'API key created',201)}catch(e){next(e)}});router.post('/:id/revoke',async(req,res,next)=>{try{const r=await prisma.apiKey.updateMany({where:{id:req.params.id,userId:req.user.sub},data:{active:false}});if(!r.count)return error(res,404,'API key not found');return success(res,{},'API key revoked')}catch(e){next(e)}});router.delete('/:id',async(req,res,next)=>{try{const r=await prisma.apiKey.deleteMany({where:{id:req.params.id,userId:req.user.sub}});if(!r.count)return error(res,404,'API key not found');return success(res,{},'API key deleted')}catch(e){next(e)}});module.exports=router;
