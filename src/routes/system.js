'use strict';
const express=require('express');const swaggerUi=require('swagger-ui-express');const asyncHandler=require('../utils/asyncHandler');const env=require('../config/env');const healthService=require('../services/health');const {getAllEndpoints,getCategorySummary}=require('../services/endpointRegistry');const {ok}=require('../utils/response');const {buildOpenApiSpec}=require('../utils/openapi');const router=express.Router();router.get('/health',asyncHandler(async(req,res)=>{const full=await healthService.getFullStatus();return ok(res,{data:{status:full.status,service:env.APP_NAME,version:env.APP_VERSION,uptime:process.uptime()}});}));router.get('/status',asyncHandler(async(req,res)=>{const full=await healthService.getFullStatus();return ok(res,{data:full});}));router.get('/version',(req,res)=>ok(res,{data:{name:env.APP_NAME,version:env.APP_VERSION,nodeEnv:env.NODE_ENV}}));router.get('/docs/endpoints.json',(req,res)=>ok(res,{data:{endpoints:getAllEndpoints().map(e=>e.meta),categories:getCategorySummary()}}));router.get('/openapi.json',(req,res)=>res.json(buildOpenApiSpec()));router.use('/docs/swagger',swaggerUi.serve,swaggerUi.setup(null,{swaggerOptions:{url:'/openapi.json'},customSiteTitle:`${env.APP_NAME} · Swagger`}));router.get('/docs',(req,res)=>res.render('docs/index',{title:'Documentación',endpoints:getAllEndpoints().map(e=>e.meta),categories:getCategorySummary(),appName:env.APP_NAME}));module.exports=router;
