const router=require('express').Router();const {requireAuth,requireRole}=require('../middleware/auth');const {success,error}=require('../utils/response');const prisma=require('../database/prisma');const {z}=require('zod');const {hashPassword}=require('../services/users');router.use(requireAuth,requireRole('admin','moderator'));router.get('/',async(req,res,next)=>{try{return success(res,await prisma.user.findMany({select:{id:true,username:true,email:true,role:true,status:true,plan:true,createdAt:true,lastLogin:true},orderBy:{createdAt:'desc'}}),'Users')}catch(e){next(e)}});router.patch('/:id',async(req,res,next)=>{try{const p=z.object({role:z.enum(['user','moderator','admin']).optional(),status:z.enum(['active','suspended','pending']).optional(),plan:z.enum(['FREE','DEVELOPER','PRO','ENTERPRISE']).optional(),password:z.string().min(8).max(128).optional()}).safeParse(req.body);if(!p.success)return error(res,400,'Invalid user data');const data={...p.data};if(data.password){data.passwordHash=await hashPassword(data.password);delete data.password}const u=await prisma.user.update({where:{id:req.params.id},data,select:{id:true,username:true,email:true,role:true,status:true,plan:true}});return success(res,u,'User updated')}catch(e){next(e)}});module.exports=router;
